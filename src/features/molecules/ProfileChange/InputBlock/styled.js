import styled from 'styled-components'
import { BaseWrapper, BaseTitle } from 'atoms'

export const ContainerWrapper = styled(BaseWrapper)`
  margin-top: 10px;
`
export const TitleWrapper = styled(BaseTitle)`
  margin-bottom: 10px;
`
